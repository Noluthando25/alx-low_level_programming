#include <stdio>
#include <time.h>

int main(void)
{
	int n;

	srand(time(0);
	n = rand() - RAND_MAX / 2;

	if (n > 0)
	{
	printf("n is positive\n");
	}
	else if (n == 0)
	{
	printf("n is 0\n");
	}
	else if (n < 0)
	{
	printf("n is negative\n");
	}

	printf("exact value of n is : %d\n", a);

	return (0);
}

