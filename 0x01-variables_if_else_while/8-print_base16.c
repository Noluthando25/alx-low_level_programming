#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Return: 0
 *
 */
int main(void)
{
	int num;
	{
		for (num = 16; num++)

		scanf("%x", &num);

		putchar("%x %d\n", num, num);

		putchar('\n');
	}
	return (0);
}
