#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * Return: 0
 *
 */
int main(void)
{
	int num;

	for (num = 10; num > 0; num++)
	{
		putchar(n);
		if (n != 10)

	putchar(',');
	putchar(' ');
	putchar('\n');

	return (0);
}
